/*
 * This page uses the URL hash to determine its initial view.
 * Hash fragments are only available in the browser, so render
 * this route on the client to avoid initially displaying Intro.
 */
export const ssr = false;
